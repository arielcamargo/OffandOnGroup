// This is how main looks like, everything minus the user line is essential for a program to run.
public class main 
{
	public static void main(String args[])
	{
		//The line below creates a user object. Its kind of like calling a method, but it does a little more, so we have to go to User.java to get more info.
		User x = new User();
	}
}
